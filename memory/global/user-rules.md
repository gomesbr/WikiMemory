---
type: global-rules
updated: 2026-04-19T04:21:45.610765Z
tags: [memory, rules, global]
---

# 🧠 Global User Rules

## ALWAYS DO

- ALWAYS think about (and implement) the solution holistically!

## NEVER DO

- DO NOT FUCK THE CODE.
- Do NOT jump into coding immediately.
- Do not answer the question.
- Do not let the full data to be more tha...
- Do not scan the whole repository unless necessary.
- Don't all questions should have those characteristics?
- Don't change anything yet.
- Don't do that, keep the same screen it was active when 'view lineage' option was selected.
- Don't ever do that.
- Don't give me step-by-step commentary please.
- Don't know if you can do something about that now or check that on second pass.
- Don't let the process to be iddle, fix the issue if any and put it to run again.
- Don't lose user info in MD files.
- Don't make code changes, just ran that for entire population.
- Don't need short updates either.
- Don't need this.
- Don't only look at critical but overall and prune whatever needs to be pruned to remove those 'duplicates', reviewed and not reviewed.
- Don't remember if we made changes on it.
- Don't show the array, but each item + value like the other elements.
- Don't stop until you find one winner.
- Don't worry about those, run the aggregation load anyways.
- Never a good practice.
- Never replace agent MD files, only append info in user file.
- do not change that file please.
- do not copy raw logs into the project.

## CONDITIONAL RULES

- Follow this operating rule: (don't remember the context of that phrase, but my question is plausibl.
- Follow this operating rule: 1) this is a new system and we must not have any historical data not properly linked.
- Follow this operating rule: 3) I realized the trade cards don't have the type of position to be opened (like, shares, options (types of options, like long call, short call, long put, leap, etc...).
- Follow this operating rule: 3) don't create user story with my inputs verbatim.
- Follow this operating rule: after your changes trigger the loop, don't expect input from me, run it solo, don't stop.
- Follow this operating rule: also make sure the deduped actors (many into 1 id) don't lose reference to their messages (aka any other table where the merged id is now referring to the new id).
- Follow this operating rule: also please don't make the boxes too far apart like you see on image 2.
- Follow this operating rule: also, implement below plan you gave me: Use this simpler first-principles design: Fix row-level actor linkage (core data contract) Every message must have: message_id, conversation_id, actor_id, actor_type, timestamp, text, reply_to_message_id (if any), source.
- Follow this operating rule: altought we don't have a winner for the retrieval method yet, do you think this can be build now, or should we focus on the retrieval loop analysis first?
- Follow this operating rule: another consideration, underlying data might change after graph is saved, so we either save graph as historical so graph will always look the same independent of data changes, or we show the graph with current data.
- Follow this operating rule: before that, fix below 2 things, but don't fix them and wait for me, go straight to the loop.
- Follow this operating rule: caveat: Don't consider current data in the platform.
- Follow this operating rule: data must be linked to actors at row level.
- Follow this operating rule: each phase MUST follow this workflow: 1.
- Follow this operating rule: external project agents must access OpenBrain through structured JSON contracts and.
- Follow this operating rule: extraction is conservative and non-LLM for v1, but the schema must remain LLM-ready so a later extractor can emit the same item model without changing downstream phases.
- Follow this operating rule: i believe there is a timeout limit if I stay away or don't perform action in the screen after few minutes.
- Follow this operating rule: i never heard of openrouter.
- Follow this operating rule: i've done that many times using the env file method and it always worked.
- Follow this operating rule: iMPORTANT OPERATING MODEL: You must understand the full system goal, but you are ONLY allowed to deeply plan ONE phase at a time.
- Follow this operating rule: if you don't understand the ask, please ask for clarification.
- Follow this operating rule: most important rule: DO NOT fix the issue by overfitting the code for that case alone.
- Follow this operating rule: no, I can instruct the agent to always use the same source_setup_id while rolling positions, thanks.
- Follow this operating rule: nothing low-confidence goes straight to “person”." Nothing low confidence should go to anywhere, so don't create rules of high-low confidence just for people, but to every entity type.
- Follow this operating rule: only after ambiguity is removed, you produce the final detailed plan for that phase DO NOT jump directly into planning.
- Follow this operating rule: openAI embedding failures must error instead of dropping to mock" - Do this.
- Follow this operating rule: pleas don't let that happen.
- Follow this operating rule: read the dos and don't rules and tell me if they should be like that.
- Follow this operating rule: removing data is always a terrible idea if you don't know what you are doing or WHY that data exists.
- Follow this operating rule: run a one-time full rebuild after you move storage to D: - not doing that for now, so don't include it in this plan.
- Follow this operating rule: save the second phase well detailed in the backlog so we don't forget to come back to it later, and start beuiding the first phase now.
- Follow this operating rule: segmentation should optimize for coherent local context, but use a balanced cutoff so segments do not grow too large for downstream classification.
- Follow this operating rule: should all date formats be the same while inserting it into db so we don't have any date compatibility issues?
- Follow this operating rule: that is the second time that this happens, always when context window compaction is happening.
- Follow this operating rule: the mock data available now is not real, so don't try to create rules retroactively based on mock data.
- Follow this operating rule: the questions ALWAYS need to come from MY point of view OR the AGENTS point of view.
- Follow this operating rule: the questions don't look bad, but the options looks finate, like they are the only options "cash, investments, debt, or net worth?"|"friends, family, or all contacts?" Maybe just don't add options, keep it open const firstMissing = String(params.plan.missingSlots[0] ??
- Follow this operating rule: the repeated 'senior software engineer' instruction appears to be a role-setting prompt for the agent rather than a project-specific rule.
- Follow this operating rule: this loooong string of cards don't look good.
- Follow this operating rule: this was your answer, which is don't see it in this plan: Perfect direction.
- Follow this operating rule: those don't add up.
- Follow this operating rule: treat requests to restart OpenBrain as a full-system restart of the entire OpenBrain infrastructure, including supporting processes such as CPU guard and SMS sending, and verify that all subsystems are healthy afterward.
- Follow this operating rule: uI skin standard (always apply by default): - Source of truth skin lives at .
- Follow this operating rule: unknown outer event types or payload subtypes do not fail normalization.
- Follow this operating rule: wHEN LOAD FINISHES, do not worry about the failed rows, execute the aggregation job anyways, got it?
- Follow this operating rule: we don't need a videa file md etc.
- Follow this operating rule: we don't need backward compatubility with anything yet.
- Follow this operating rule: when compacting context data from this window, always bring the full plan (we are about to create that in a sec).
- Follow this operating rule: you will: - create all files - install dependencies - ensure project runs - verify startup Do not stop until build is complete.
- Keep responses concise by default and avoid unnecessary process narration when token budget matters.
- Validate memory and extraction changes against real data before treating them as correct.
- When the user asks for repository changes, commit and push without asking again unless credentials or conflicts block the action.
- When the user asks for repository changes, commit and push without asking again unless credentials or conflicts block the action.
- When the user asks for repository changes, commit and push without asking again unless credentials or conflicts block the action.

## CONFIRMED RULES (EXPLICIT)

- 76 explicit rule(s) are listed above by behavior bucket.

## INFERRED RULES (REVIEWABLE)

- Follow this operating rule: the repeated 'senior software engineer' instruction appears to be a role-setting prompt for the agent rather than a project-specific rule. (confidence: low; source_count: 7)
- Keep responses concise by default and avoid unnecessary process narration when token budget matters. (confidence: strong; source_count: 9)
- Validate memory and extraction changes against real data before treating them as correct. (confidence: strong; source_count: 9)
- When the user asks for repository changes, commit and push without asking again unless credentials or conflicts block the action. (confidence: strong; source_count: 15)

## PROVENANCE

- Derived from clear or repeated user instructions.
